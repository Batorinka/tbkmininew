on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Keep the workflow revision in sync with the ZMK pin in config/west.yml
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.2.1
