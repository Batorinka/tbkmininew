manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      # Pinned: shield uses pre-physical-layout APIs; last known good
      # builds were contemporary with the v0.2.x line (Feb 2025).
      revision: v0.2.1
      import: app/west.yml
  self:
    path: config
